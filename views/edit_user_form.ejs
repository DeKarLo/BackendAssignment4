<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit User</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/style.css" />
    <style>
        .user-box {
            border: 1px solid #ced4da;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <%- include("./partials/navbar") %>
    <div class="container field bg-white mt-4">
        <% if(error) { %>
        <div class="alert alert-danger" role="alert"><%= error %></div>
        <% } %> <% if(message) { %>
        <div class="alert alert-success" role="alert"><%= message %></div>
        <% } %>
        <h1><% if (language === 'ru') { %> Редактировать пользователя <% } else { %> Edit User <% } %></h1>
        <div class="user-box">
            <form action="/admin/users/edit/<%= user._id %>" method="POST">
                <div class="form-group">
                    <label for="username">Username:</label>
                    <input type="text" class="form-control" id="username" name="username" value="<%= user.username %>" required />
                </div>
                <div class="form-group">
                    <label for="isAdmin">Admin Status:</label>
                    <select class="form-control" id="isAdmin" name="isAdmin" required>
                        <option value="true" <% if (user.isAdmin === true) { %> selected <% } %>>Yes</option>
                        <option value="false" <% if (user.isAdmin === false) { %> selected <% } %>>No</option>
                    </select>
                </div>
                <!-- Add other fields as needed -->

                <button type="submit" class="btn btn-primary">
                    <% if (language === 'ru') { %> Обновить информацию <% } else { %> Update Info <% } %>
                </button>
            </form>
        </div>
    </div>
    <%- include("./partials/footer.ejs") %>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
    <script src="/js/language.js"></script>
</body>
</html>
